apiVersion: v1
kind: Service
metadata:
  name: ecommerce-api
  namespace: ecommerce
  labels:
    app: ecommerce-api
spec:
  type: NodePort
  selector:
    app: ecommerce-api
  ports:
  - port: 8000
    targetPort: 8000
    nodePort: 30800
    name: http